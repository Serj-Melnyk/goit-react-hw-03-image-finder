import { ButtonMore } from "./ButtonStyled"

export const Button = ({text}) => {
    return (

        
        <div>
             <ButtonMore type="button">{ text }</ButtonMore>
        </div>

       
    )
}