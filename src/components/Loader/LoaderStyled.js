import styled from 'styled-components';
import { ThreeDots as LoadDots } from 'react-loader-spinner';

export const ThreeDots = styled(LoadDots)`
  display: flex;
  justify-items: center;
  align-items: center;
  color: red;
`;
